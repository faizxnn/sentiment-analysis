<Window x:Class="SentimentAnalysisTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SentimentAnalysisTool"
        xmlns:controls="clr-namespace:SentimentAnalysisTool.Controls"
        mc:Ignorable="d"
        Title="Sentiment Analysis Tool" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Sentiment Analysis Tool" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Margin="0,0,0,20"/>

        <controls:PlaceholderTextBox x:Name="InputTextBox" 
                 Grid.Row="1" 
                 TextWrapping="Wrap" 
                 AcceptsReturn="True" 
                 VerticalScrollBarVisibility="Auto"
                 Padding="10"
                 FontSize="14"
                 Margin="0,0,0,20"
                 PlaceholderText="Enter text to analyze..."/>

        <Button x:Name="AnalyzeButton" 
                Grid.Row="2" 
                Content="Analyze Sentiment" 
                HorizontalAlignment="Center" 
                Padding="15,10" 
                Margin="0,0,0,20"
                Click="AnalyzeButton_Click"/>

        <Border Grid.Row="3" 
                BorderBrush="#DDDDDD" 
                BorderThickness="1" 
                Padding="15" 
                Background="#F5F5F5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Sentiment:" FontWeight="Bold" Margin="0,0,10,5"/>
                <TextBlock Grid.Row="0" Grid.Column="1" x:Name="SentimentResultTextBlock" Text="N/A" Margin="0,0,0,5"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Score:" FontWeight="Bold" Margin="0,0,10,5"/>
                <TextBlock Grid.Row="1" Grid.Column="1" x:Name="ScoreResultTextBlock" Text="N/A" Margin="0,0,0,5"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Probability:" FontWeight="Bold" Margin="0,0,10,0"/>
                <TextBlock Grid.Row="2" Grid.Column="1" x:Name="ProbabilityResultTextBlock" Text="N/A"/>
            </Grid>
        </Border>
    </Grid>
</Window> 